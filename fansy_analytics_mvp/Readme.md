# 🚀 Fansy Analytics MVP

**Real-time мониторинг и анализ legacy-системы Fansy**

Минимальный рабочий прототип для отслеживания SQL-запросов, вызовов функций и визуализации зависимостей.

---

## 📋 Что включено

### Backend
- ✅ **Firebird SQL Tracer** - перехват и логирование SQL-запросов
- ✅ **Function Call Logger** - отслеживание вызовов функций FANSY-SCRIPT
- ✅ **Dependency Graph Builder** - построение графа зависимостей
- ✅ **Flask Web Server** - REST API + Server-Sent Events (real-time)

### Frontend
- ✅ **Dashboard** - веб-интерфейс с real-time обновлениями
- ✅ **Графики** - статистика по модулям и функциям (Chart.js)
- ✅ **Граф зависимостей** - интерактивная визуализация (vis.js)
- ✅ **История вызовов** - real-time лента событий

---

## 🛠️ Установка

### Требования
- Python 3.8+
- pip

### Шаг 1: Установка зависимостей

```bash
pip install flask flask-cors fdb networkx
```

**Список всех зависимостей:**
- `flask` - веб-фреймворк
- `flask-cors` - CORS для разработки
- `fdb` - драйвер Firebird
- `networkx` - анализ графов

### Шаг 2: Настройка подключения к Firebird

Откройте `backend/firebird_tracer.py` и измените настройки:

```python
tracer = FirebirdTracer(
    host='localhost',           # ваш хост
    database='C:\\path\\to\\BAL_META.FDB',  # путь к базе
    user='SYSDBA',              # пользователь
    password='masterkey'        # пароль
)
```

---

## 🚀 Запуск

### Быстрый старт (демо с тестовыми данными)

```bash
cd backend
python web_server.py
```

Откройте браузер: **http://localhost:5000**

### Запуск с реальной базой Firebird

1. **Настройте подключение** в `firebird_tracer.py`
2. **Запустите сервер:**

```bash
python web_server.py
```

3. **Откройте dashboard** в браузере

---

## 📊 Использование

### Web Dashboard

```
http://localhost:5000
```

**Функции:**
- 📈 Статистика вызовов в real-time
- 🔄 Мониторинг активных вызовов
- 📜 История с ошибками
- 🕸️ Интерактивный граф зависимостей
- 🧪 Тестовая симуляция

### REST API

**Статистика вызовов:**
```bash
curl http://localhost:5000/api/function-calls/stats
```

**История вызовов:**
```bash
curl http://localhost:5000/api/function-calls/history?limit=20
```

**Активные вызовы:**
```bash
curl http://localhost:5000/api/function-calls/active
```

**Информация о функции:**
```bash
curl http://localhost:5000/api/graph/function/Get_NDFL_Nach
```

**Подграф вокруг функции:**
```bash
curl http://localhost:5000/api/graph/subgraph/OP_P_NDFL_PRC_BODY?depth=2
```

**Путь между функциями:**
```bash
curl "http://localhost:5000/api/graph/path?from=OP_P_NDFL_PRC_BODY&to=Get_Rate"
```

### Server-Sent Events (Real-time)

```javascript
const eventSource = new EventSource('http://localhost:5000/api/events');

eventSource.onmessage = (event) => {
    const data = JSON.parse(event.data);
    console.log('Event:', data);
};
```

---

## 📁 Структура проекта

```
fansy_analytics_mvp/
├── backend/
│   ├── firebird_tracer.py       # SQL tracer
│   ├── function_logger.py       # Function call logger
│   ├── graph_builder.py         # Dependency graph builder
│   └── web_server.py            # Flask web server
├── frontend/
│   └── dependency_graph.html    # Standalone graph viewer
├── data/
│   └── *.json                   # Экспорты данных
├── logs/
│   └── *.json                   # Логи
└── docs/
    └── README.md                # Эта документация
```

---

## 🔧 Примеры использования

### 1. Перехват SQL-запросов

```python
from firebird_tracer import FirebirdTracer

tracer = FirebirdTracer(
    host='localhost',
    database='BAL_META.FDB',
    user='SYSDBA',
    password='masterkey'
)

tracer.connect()

# Выполняем запрос с трейсингом
result = tracer.trace_query("""
    SELECT FUNC_NAME, APP_NUM 
    FROM DICMETAF 
    WHERE FUNC_NAME LIKE '%NDFL%'
""")

print(f"Время выполнения: {result['query_info']['duration']}s")
print(f"Найдено строк: {result['query_info']['rows_affected']}")

# Статистика
stats = tracer.get_stats()
print(f"Всего запросов: {stats['total_queries']}")

tracer.disconnect()
```

### 2. Логирование вызовов функций

```python
from function_logger import FunctionCallLogger

logger = FunctionCallLogger()

# Начало вызова
call_id = logger.start_call('_F_SPECTRE', 'Get_NDFL_Nach', {
    'b_date': '2025-01-01',
    'e_date': '2025-12-31',
    'investor': 12345
})

# ... выполнение функции ...

# Завершение вызова
logger.end_call(call_id, result=15000.50)

# Статистика
stats = logger.get_stats()
print(f"Всего вызовов: {stats['total_calls']}")
print(f"Среднее время: {stats['avg_time']}s")

# Дерево вызовов
tree = logger.get_call_tree()
```

### 3. Построение графа зависимостей

```python
from graph_builder import DependencyGraphBuilder

builder = DependencyGraphBuilder()

# Добавляем функции
builder.add_function('OP_P_NDFL_PRC_BODY', '_F_SPECTRE', 
    [('doc_id', 'int'), ('dir_id', 'int')],
    'Расчет НДФЛ', 2236)

builder.add_function('Get_NDFL_Nach', '_F_SPECTRE',
    [('b_date', 'DATE'), ('e_date', 'DATE')],
    'Начисленный НДФЛ', 150)

# Добавляем вызов
builder.add_call('OP_P_NDFL_PRC_BODY', 'Get_NDFL_Nach', line_number=2192)

# Получаем подграф
subgraph = builder.get_subgraph('OP_P_NDFL_PRC_BODY', depth=2)

# Экспорт в HTML
builder.export_to_html('graph.html', focus_func='OP_P_NDFL_PRC_BODY')
```

---

## 🎨 Скриншоты функций

### Dashboard
- **Real-time статистика** - обновляется автоматически
- **Графики Chart.js** - по модулям и функциям
- **Лента событий** - все вызовы в real-time

### Граф зависимостей
- **Интерактивный** - можно таскать узлы
- **Цветовая кодировка** - по модулям
- **Размер узлов** - по количеству строк кода
- **Толщина связей** - по частоте вызовов

---

## 🔍 Отладка проблем

### Проблема: Не подключается к Firebird

**Решение:**
1. Проверьте, что Firebird запущен
2. Проверьте путь к базе (должен быть полный путь)
3. Проверьте права доступа
4. На Windows используйте двойные слэши: `C:\\path\\to\\db.fdb`

### Проблема: CORS ошибки

**Решение:** Flask-CORS уже настроен, но если проблемы:

```python
from flask_cors import CORS
CORS(app, resources={r"/api/*": {"origins": "*"}})
```

### Проблема: Real-time не работает

**Решение:** SSE требует поддержки в браузере. Используйте Chrome/Firefox.

---

## 📈 Следующие шаги (Phase 2)

После успешного тестирования MVP:

### Краткосрочные улучшения (1-2 недели)
- [ ] Аутентификация и авторизация
- [ ] Сохранение истории в PostgreSQL
- [ ] Фильтры и поиск в истории
- [ ] Экспорт отчётов в PDF/Excel
- [ ] Алерты по медленным запросам

### Среднесрочные (1 месяц)
- [ ] Автоматический парсинг FANSY-SCRIPT из META
- [ ] Сравнение версий функций
- [ ] Трейсинг на production (безопасный режим)
- [ ] Интеграция с Jira (создание тикетов на ошибки)

### Долгосрочные (2-3 месяца)
- [ ] ML для предсказания проблем
- [ ] Автогенерация тестов
- [ ] Performance profiling
- [ ] Кластеризация для больших систем

---

## 💡 Советы для QA-отдела

### Как использовать для тестирования

1. **Запустите dashboard** перед тестами
2. **Выполните тестовый сценарий** в Fansy
3. **Наблюдайте real-time** какие функции вызываются
4. **Проверьте граф** - все ли зависимости корректны
5. **Найдите медленные вызовы** в разделе "История"
6. **Экспортируйте данные** для отчёта

### Типичные паттерны ошибок

**Медленные запросы:**
- Ищите в `/api/function-calls/slow?threshold=1.0`
- Проверяйте SQL запросы к большим таблицам

**Циклические зависимости:**
- `/api/graph/circular`
- Могут привести к deadlock

**Функции с ошибками:**
- Фильтр в истории по статусу ERROR
- Смотрите параметры, которые были переданы

---

## 🤝 Контрибьюция

### Как добавить новый функционал

1. Форкните проект
2. Создайте feature branch
3. Добавьте код + тесты
4. Создайте Pull Request

### Структура коммитов

```
feat: добавлена фильтрация по модулям
fix: исправлена ошибка в парсере SQL
docs: обновлена документация API
```

---

## 📄 Лицензия

MIT License - используйте свободно для внутренних нужд.

---

## 🎯 Feedback

Если что-то не работает или есть идеи по улучшению:
1. Создайте Issue
2. Опишите проблему с примером
3. Приложите логи

---

## ⚡ Быстрые команды

```bash
# Установка
pip install flask flask-cors fdb networkx

# Запуск
cd backend
python web_server.py

# Тестирование API
curl http://localhost:5000/api/function-calls/stats

# Экспорт данных
curl http://localhost:5000/api/graph/stats > graph_stats.json
```

---

**Версия:** 1.0 MVP  
**Дата:** 2026-02-05  
**Статус:** ✅ Ready for testing
